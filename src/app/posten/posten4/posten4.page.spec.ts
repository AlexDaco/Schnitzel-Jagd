import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Posten4Page } from './posten4.page';

describe('Posten4Page', () => {
  let component: Posten4Page;
  let fixture: ComponentFixture<Posten4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Posten4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
