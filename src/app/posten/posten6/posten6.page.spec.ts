import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Posten6Page } from './posten6.page';

describe('Posten6Page', () => {
  let component: Posten6Page;
  let fixture: ComponentFixture<Posten6Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Posten6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
